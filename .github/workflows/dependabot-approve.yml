name: Dependabot Approve

on:
  pull_request:

permissions:
  pull-requests: write

jobs:
  approve:
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    steps:
      - name: Fetch Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      # Patch and minor updates are auto-approved and left for the
      # Dependabot Auto-merge workflow to merge once CI is green.
      # Branch protection requires 1 approving review; dependabot[bot] can't
      # approve its own PR, so nothing was ever satisfying that requirement.
      # github-actions[bot] is a distinct actor from dependabot[bot], so it
      # can supply the approval.
      - name: Auto-approve patch and minor updates
        if: |
          steps.metadata.outputs.update-type == 'version-update:semver-patch' ||
          steps.metadata.outputs.update-type == 'version-update:semver-minor'
        run: gh pr review --approve "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
